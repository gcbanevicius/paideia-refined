<% content_for :body_content_title do %>
  <%= @carousel_image.title %>
<% end %>

<% content_for :body do %>
  <section>
    <h1>Title</h1>
    <p>
      <%=raw @carousel_image.title %>
    </p>
  </section>
  <section>
    <h1>Image</h1>
    <p>
      <%= image_fu @carousel_image.image, nil %>
    </p>
  </section>
  <section>
    <h1>Caption</h1>
    <p>
      <%=raw @carousel_image.caption %>
    </p>
  </section>
  <section>
    <h1>Link</h1>
    <p>
      <%=raw @carousel_image.link %>
    </p>
  </section>
<% end %>

<% content_for :side_body do %>
  <aside>
    <h2><%= t('.other') %></h2>
    <ul id="carousel_images">
      <% @carousel_images.each do |carousel_image| %>
        <li>
          <%= link_to carousel_image.title, refinery.carousel_images_carousel_image_path(carousel_image) %>
        </li>
      <% end %>
    </ul>
  </aside>
<% end %>

<%= render '/refinery/content_page' %>
