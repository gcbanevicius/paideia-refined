<h1>Webinar Login</h1>

<%= flash[:notice] %>

<%= form_tag("attempt_login") do %>

  <%= label_tag(:username) %>
  <%= text_field_tag(:username) %>
  
  <%= label_tag(:password) %>
  <%= password_field_tag(:password) %>
    
  <%= submit_tag("Sign In") %>
  
<% end %>
